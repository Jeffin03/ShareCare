{
  "name": "sharecare",
  "version": "1.0.0",
  "description": "Medicine request portal and community platform for diabetes patients",
  "scripts": {
    "build": "node scripts/build-env.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "seed": "node scripts/seed.js"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "firebase-admin": "^12.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}